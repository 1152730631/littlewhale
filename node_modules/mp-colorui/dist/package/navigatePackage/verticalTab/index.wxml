<block wx:if="{{$taroCompReady}}">
    <view>
        <cl-vertical-tab compid="{{$compid__1367}}">
            <view>
                <view id="{{item.id}}" wx:key="name" wx:for="{{tabs}}" wx:for-item="item" wx:for-index="_anonIdx2">
                    <cl-vertical-tab-cell>{{item.name}}</cl-vertical-tab-cell>
                </view>
            </view>
        </cl-vertical-tab>
    </view>
</block>