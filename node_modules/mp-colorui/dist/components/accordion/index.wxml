<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <view class="cu-item" bindtap="anonymousFunc0">
            <view class="flex justify-between align-center" style="{{anonymousState__temp3}}">
                <view>{{title}}</view>
                <view style="{{anonymousState__temp4}}">
                    <cl-icon compid="{{$compid__854}}"></cl-icon>
                </view>
            </view>
        </view>
        <view id="{{id}}" class="bg-white">
            <scroll-view scroll-y="{{true}}" style="{{anonymousState__temp5}}">
                <slot></slot>
            </scroll-view>
        </view>
    </view>
</block>