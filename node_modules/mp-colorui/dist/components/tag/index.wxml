<block wx:if="{{$taroCompReady}}">
    <block>
        <block wx:if="{{tags.length > 1}}">
            <block>
                <block wx:if="{{tags.length > 0}}">
                    <view class="{{_$anonymousState__temp5}}" style="{{_$anonymousState__temp6}}">
                        <view wx:key="text" class="{{tag.$loopState__temp8}}" bindtap="onClick" wx:for="{{loopArray155}}" wx:for-item="tag" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{index}}">
                            <block wx:if="{{tag.$original.icon}}">
                                <cl-icon compid="{{tag.$compid__1752}}"></cl-icon>
                            </block><text style="{{tag.$loopState__temp12}}">{{tag.$original.text}}</text>
                        </view>
                    </view>
                </block>
            </block>
        </block>
        <block wx:else>
            <block>
                <block wx:if="{{tags.length > 0}}">
                    <view class="{{_$anonymousState__temp}}" style="{{_$anonymousState__temp2}}">
                        <block wx:if="{{_$tag.icon}}">
                            <cl-icon compid="{{$compid__1753}}"></cl-icon>
                        </block><text style="{{_$anonymousState__temp4}}">{{_$tag.text}}</text>
                    </view>
                </block>
            </block>
        </block>
    </block>
</block>