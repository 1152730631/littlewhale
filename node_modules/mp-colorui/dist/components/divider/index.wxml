<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <cl-layout compid="{{$compid__1592}}">
            <view class="{{colorClassName + ' ' + sizeClassName + ' cu-divider'}}">
                <slot></slot>
            </view>
        </cl-layout>
    </view>
</block>