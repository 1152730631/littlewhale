<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
        <view wx:key="title" class="{{item.$loopState__temp2}}" bindtap="anonymousFunc0" wx:for="{{loopArray64}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}">
            <view class="content flex justify-between">
                <view class="content flex align-center">
                    <block wx:if="{{item.$original.icon && item.$original.icon.iconName}}">
                        <view class="margin-right-xs flex align-center">
                            <cl-icon compid="{{item.$compid__832}}"></cl-icon>
                        </view>
                    </block>
                    <block wx:if="{{item.$original.imgUrl}}">
                        <image src="{{item.$original.imgUrl}}" class="png margin-right-xs" mode="aspectFit"></image>
                    </block><text class="{{'' + TEXT_COLOR_LIST[item.$original.titleColor || 'black']}}">{{item.$original.title}}</text>
                </view>
                <view class="flex align-center">{{item.$original.value}}</view>
            </view>
        </view>
    </view>
</block>