<block wx:if="{{$taroCompReady}}">
    <block>
        <block wx:if="{{showImage}}">
            <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" catchtap="anonymousFunc0">
                <view class="cu-curtain__content" bindtap="anonymousFunc1">
                    <slot></slot>
                    <view class="{{closeBtnPosition}}" catchtap="anonymousFunc2">
                        <cl-icon compid="{{$compid__1591}}"></cl-icon>
                    </view>
                </view>
            </view>
        </block>
        <block wx:else>
            <view></view>
        </block>
    </block>
</block>