<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <scroll-view scroll-y="{{true}}" class="{{'DrawerPage ' + (props.show ? 'show' : '')}}">
            <slot name="page"></slot>
        </scroll-view>
        <view class="{{'DrawerClose ' + (props.show ? 'show' : '')}}" bindtap="anonymousFunc0"><text class="cuIcon-pullright"></text>
        </view>
        <scroll-view scroll-y="{{true}}" class="{{'DrawerWindow ' + (props.show ? 'show' : '')}}">
            <slot name="drawer"></slot>
        </scroll-view>
    </view>
</block>