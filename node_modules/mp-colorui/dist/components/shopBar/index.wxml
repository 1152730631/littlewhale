<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
        <view wx:key="id" class="ClShopBar__tab" wx:for="{{loopArray115}}" wx:for-item="item" wx:for-index="index">
            <cl-button compid="{{item.$compid__1593}}">
                <view class="action" style="{{item.$loopState__temp2}}">
                    <view class="{{'' + (item.$original.icon ? 'cuIcon-' + item.$original.icon : '')}}">
                        <block wx:if="{{item.$original.badge !== false}}">
                            <view class="cu-tag badge">{{item.$original.badge === true ? "" : item.$original.badge}}</view>
                        </block>
                    </view>
                    <view>{{item.$original.title}}</view>
                </view>
            </cl-button>
        </view>
        <view wx:key="id" class="{{BG_COLOR_LIST[item.$original.bgColor || 'red'] + ' submit'}}" bindtap="anonymousFunc1" wx:for="{{loopArray116}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey2}}">{{item.$original.text}}</view>
    </view>
</block>