<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
        <scroll-view scroll-y="{{true}}" scroll-with-animation="{{true}}" class="VerticalNav nav" style="{{anonymousState__temp5}}" scroll-top="{{(verticalNavTop - 1) * screenPercent * 100}}" enable-back-to-top="{{props.backTop}}">
            <view wx:key="cu_vertical_tab_id" class="{{'cu-item ' + (current === item.$original.id ? 'cur' : '')}}" bindtap="anonymousFunc0" wx:for="{{loopArray84}}" wx:for-item="item" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}">{{item.$original.name}}</view>
        </scroll-view>
        <block>
            <block wx:if="{{isH5}}">
                <scroll-view scroll-y="{{true}}" scroll-with-animation="{{true}}" scroll-animation-duration="{{200}}" style="{{anonymousState__temp2}}" bindscroll="anonymousFunc2" scroll-top="{{scrollContent}}" enable-back-to-top="{{props.backTop}}">
                    <slot></slot>
                </scroll-view>
            </block>
            <block wx:else>
                <scroll-view scroll-y="{{true}}" scroll-with-animation="{{true}}" scroll-animation-duration="{{200}}" style="{{anonymousState__temp}}" scroll-into-view="{{scrollId}}" bindscroll="anonymousFunc1" scroll-top="{{isAliPay ? undefined : scrollContent}}" enable-back-to-top="{{props.backTop}}">
                    <slot></slot>
                </scroll-view>
            </block>
        </block>
    </view>
</block>