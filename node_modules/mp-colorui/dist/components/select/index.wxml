<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <view class="title">{{title || ""}}</view>
        <block wx:if="{{props.mode === 'selector'}}">
            <picker mode="selector" range="{{selector.range}}" range-key="{{selector.rangeKey}}" value="{{selector.value || 0}}" bindchange="anonymousFunc0" bindcancel="anonymousFunc1" disabled="{{props.disabled}}" class="longSelect">
                <view class="picker">{{selected}}</view>
            </picker>
        </block>
        <block wx:if="{{props.mode === 'multiSelector' && !isAliPay}}">
            <picker mode="multiSelector" range="{{mutiSelector.range}}" range-key="{{mutiSelector.rangeKey}}" value="{{mutiSelector.value}}" bindchange="anonymousFunc2" bindcolumnchange="anonymousFunc3" bindcancel="anonymousFunc4" disabled="{{props.disabled}}" class="longSelect">
                <view class="picker">{{mutiSelected}}</view>
            </picker>
        </block>
        <block wx:if="{{props.mode === 'multiSelector' && isAliPay}}">
            <alipay-muti-select compid="{{$compid__1576}}"></alipay-muti-select>
        </block>
        <block wx:if="{{props.mode === 'time'}}">
            <picker mode="time" value="{{timeSelector.value}}" start="{{timeSelector.start}}" end="{{timeSelector.end}}" bindchange="anonymousFunc5" bindcancel="anonymousFunc6" disabled="{{props.disabled}}" class="longSelect">
                <view class="picker">{{timeSelected}}</view>
            </picker>
        </block>
        <block wx:if="{{props.mode === 'date'}}">
            <picker mode="date" value="{{dateSelector.value}}" start="{{dateSelector.start}}" end="{{dateSelector.end}}" fields="{{dateSelector.fields}}" bindcancel="anonymousFunc7" bindchange="anonymousFunc8" class="longSelect">
                <view class="picker">{{dateSelected}}</view>
            </picker>
        </block>
        <block wx:if="{{props.mode === 'region'}}">
            <picker mode="multiSelector" range="{{areaData}}" range-key="value" value="{{tempSelect}}" bindchange="anonymousFunc9" bindcolumnchange="anonymousFunc10" bindcancel="anonymousFunc11" disabled="{{props.disabled}}" class="longSelect">
                <view class="picker">{{regionSelected}}</view>
            </picker>
        </block>
    </view>
</block>