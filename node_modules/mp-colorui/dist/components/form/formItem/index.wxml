<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <slot></slot>
        <block wx:if="{{err}}">
            <view class="cu-formItem-err-tip">{{tip}}</view>
        </block>
        <block wx:if="{{required}}">
            <view class="cu-formItem-required"></view>
        </block>
    </view>
</block>