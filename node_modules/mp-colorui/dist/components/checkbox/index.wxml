<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}">
        <block>
            <block wx:if="{{!isWeApp}}">
                <cl-checkbox-h-5 compid="{{$compid__861}}"></cl-checkbox-h-5>
            </block>
            <block wx:else>
                <checkbox-group class="{{'' + (type === 'form' ? 'block' : '')}}" bindchange="anonymousFunc0">
                    <block>
                        <block wx:if="{{type === 'form'}}">
                            <view class="cu-form-group">
                                <view class="title">{{title}}</view>
                                <view class="{{directionClassName}}">
                                    <view class="padding-xs" wx:key="value" wx:for="{{loopArray72}}" wx:for-item="item" wx:for-index="_anonIdx2">
                                        <block wx:if="{{item.$original.key}}"><text class="padding-right-sm">{{item.$original.key}}</text>
                                        </block>
                                        <checkbox class="{{item.$loopState__temp2}}" value="{{item.$original.value || ''}}" checked="{{item.$original.checked}}" disabled="{{item.$original.disabled}}"></checkbox>
                                    </view>
                                </view>
                            </view>
                        </block>
                        <block wx:else>
                            <view class="{{directionClassName}}">
                                <view class="padding-xs" wx:key="value" wx:for="{{loopArray72}}" wx:for-item="item" wx:for-index="_anonIdx2">
                                    <block wx:if="{{item.$original.key}}"><text class="padding-right-sm">{{item.$original.key}}</text>
                                    </block>
                                    <checkbox class="{{item.$loopState__temp2}}" value="{{item.$original.value || ''}}" checked="{{item.$original.checked}}" disabled="{{item.$original.disabled}}"></checkbox>
                                </view>
                            </view>
                        </block>
                    </block>
                </checkbox-group>
            </block>
        </block>
    </view>
</block>