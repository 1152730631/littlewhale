<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp7}}" style="{{anonymousState__temp8}}">
        <view bindtap="anonymousFunc0" wx:key="$loopState__temp2" class="{{'action ' + (item.$original.action ? 'add-action' : '') + ' ' + (activeIndex === index ? activeColorClassName : '')}}" wx:for="{{loopArray80}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}">
            <view class="{{item.$loopState__temp4}}">
                <block wx:if="{{item.$original.img}}">
                    <image mode="aspectFit" src="{{item.$original.img}}"></image>
                </block>
                <block wx:if="{{item.$original.badge !== false}}">
                    <view class="cu-tag badge">{{item.$loopState__temp6}}</view>
                </block>
            </view>
            <view>{{item.$original.title}}</view>
        </view>
    </view>
</block>