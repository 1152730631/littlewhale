<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp6}}" style="{{anonymousState__temp7}}">
        <block>
            <block wx:if="{{!isWeApp}}">
                <h-5-radio compid="{{$compid__862}}"></h-5-radio>
            </block>
            <block wx:else>
                <block>
                    <block wx:if="{{type === 'list'}}">
                        <template is="renderClosureListComponent" data="{{...anonymousState__temp}}"></template>
                    </block>
                    <block wx:else>
                        <radio-group bindchange="anonymousFunc1" class="{{'' + (type === 'form' ? 'block' : '')}}">
                            <block>
                                <block wx:if="{{type === 'form'}}">
                                    <view class="cu-form-group">
                                        <view class="title">{{title}}</view>
                                        <view class="{{directionClassName}}">
                                            <view class="padding-xs" wx:key="$loopState__temp3" bindtap="anonymousFunc0" wx:for="{{loopArray74}}" wx:for-item="item" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}"><text class="padding-right-sm">{{item.$original.key}}</text>
                                                <radio class="{{item.$loopState__temp5}}" checked="{{item.$original.value === checkedValue}}" value="{{item.$original.value || ''}}" disabled="{{props.disabled}}"></radio>
                                            </view>
                                        </view>
                                    </view>
                                </block>
                                <block wx:else>
                                    <view class="{{directionClassName}}">
                                        <view class="padding-xs" wx:key="$loopState__temp3" bindtap="anonymousFunc0" wx:for="{{loopArray74}}" wx:for-item="item" wx:for-index="__index0" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}"><text class="padding-right-sm">{{item.$original.key}}</text>
                                            <radio class="{{item.$loopState__temp5}}" checked="{{item.$original.value === checkedValue}}" value="{{item.$original.value || ''}}" disabled="{{props.disabled}}"></radio>
                                        </view>
                                    </view>
                                </block>
                            </block>
                        </radio-group>
                    </block>
                </block>
            </block>
        </block>
    </view>
</block>
<template name="renderClosureListComponent">
    <block>
        <list-radio compid="{{$compid__863}}"></list-radio>
    </block>
</template>