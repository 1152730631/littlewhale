<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp3}}" style="{{anonymousState__temp4}}" bindtap="anonymousFunc1">
        <view class="cu-dialog" catchtap="anonymousFunc2">
            <block>
                <block wx:if="{{custom}}">
                    <slot name="title"></slot>
                </block>
                <block wx:else>
                    <view class="{{'cu-bar justify-end ' + titleBgColorClassName}}">
                        <view class="content">{{title}}</view>
                        <block wx:if="{{close}}">
                            <view class="action" bindtap="anonymousFunc3"><text class="cuIcon-close text-black"></text>
                            </view>
                        </block>
                    </view>
                </block>
            </block>
            <view class="{{anonymousState__temp5}}">
                <slot></slot>
            </view>
            <block>
                <block wx:if="{{custom}}">
                    <slot name="action"></slot>
                </block>
                <block wx:else>
                    <view class="{{'cu-bar ' + actionColor}}">
                        <view class="{{(index > 0 ? 'solid-left' : '') + ' action margin-0 flex-sub'}}" wx:key="$loopState__temp2" bindtap="anonymousFunc0" wx:for="{{loopArray68}}" wx:for-item="item" wx:for-index="index" data-e-tap-so="this" data-e-tap-a-a="{{item._$indexKey}}"><text class="{{'' + (item.$original.color ? TEXT_COLOR_LIST[item.$original.color] : '')}}">{{item.$original.text}}</text>
                        </view>
                    </view>
                </block>
            </block>
        </view>
    </view>
</block>