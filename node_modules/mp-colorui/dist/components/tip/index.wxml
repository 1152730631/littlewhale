<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp5}}" style="{{anonymousState__temp6}}">
        <block>
            <block wx:if="{{isH5}}">
                <cl-tip-h-5 compid="{{$compid__1569}}"></cl-tip-h-5>
            </block>
            <block wx:else>
                <view id="content" bindtap="anonymousFunc0" bindtouchstart="anonymousFunc1" bindtouchend="anonymousFunc2" class="{{'cl-tip ' + (showTip ? 'cl-tip__showZindex' : '')}}" style="{{anonymousState__temp}}">
                    <view id="message" class="{{'cl-tip__content  shadow ' + (showTip ? 'show' : 'hide')}}" style="{{anonymousState__temp2}}">
                        <view class="{{'cl-tip__arrow ' + props.direction + ' ' + BG_COLOR_LIST[props.bgColor || 'white'] + ' ' + (showTip ? 'showArrow' : 'hideArrow')}}" style="{{anonymousState__temp3}}"></view>
                        <view class="{{'cl-tip__message ' + BG_COLOR_LIST[props.bgColor || 'white']}}">
                            <view style="{{anonymousState__temp4}}">
                                <cl-text compid="{{$compid__1568}}"></cl-text>
                                <view catchtap="anonymousFunc3">
                                    <slot name="message"></slot>
                                </view>
                            </view>
                        </view>
                    </view>
                    <slot></slot>
                </view>
            </block>
        </block>
    </view>
</block>