<block wx:if="{{$taroCompReady}}">
    <view id="{{id}}" bindtap="anonymousFunc0" bindtouchstart="anonymousFunc1" bindtouchend="anonymousFunc2" class="{{'cl-tip ' + (showTip ? 'cl-tip__showZindex' : '')}}" style="{{anonymousState__temp}}">
        <view id="{{idMessage}}" class="{{'cl-tip__content  shadow ' + (showTip ? 'show' : 'hide') + ' ' + (showTip ? 'true' : 'false')}}" style="{{anonymousState__temp2}}">
            <view class="{{'cl-tip__arrow ' + props.direction + ' ' + BG_COLOR_LIST[props.bgColor || 'white'] + ' ' + (showTip ? 'showArrow' : 'hideArrow')}}" style="{{anonymousState__temp3}}"></view>
            <view class="{{'cl-tip__message ' + BG_COLOR_LIST[props.bgColor || 'white']}}">
                <view style="{{anonymousState__temp4}}">
                    <cl-text compid="{{$compid__1570}}"></cl-text>
                    <view catchtap="anonymousFunc3">
                        <slot name="message"></slot>
                    </view>
                </view>
            </view>
        </view>
        <slot></slot>
    </view>
</block>