<block wx:if="{{$taroCompReady}}">
    <block>
        <block wx:if="{{props.direction === 'left'}}">
            <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}" catchtap="anonymousFunc0">
                <view class="cu-dialog basis-lg" catchtap="anonymousFunc1" style="{{anonymousState__temp3}}">
                    <slot></slot>
                </view>
            </view>
        </block>
        <block wx:else>
            <block>
                <block wx:if="{{props.direction === 'right'}}">
                    <view class="{{anonymousState__temp4}}" style="{{anonymousState__temp5}}" catchtap="anonymousFunc2">
                        <view class="cu-dialog basis-lg" catchtap="anonymousFunc3" style="{{anonymousState__temp6}}">
                            <slot></slot>
                        </view>
                    </view>
                </block>
                <block wx:else>
                    <view class="{{anonymousState__temp7}}" style="{{anonymousState__temp8}}" catchtap="anonymousFunc4">
                        <view class="cu-dialog" style="{{anonymousState__temp9}}" catchtap="anonymousFunc5">
                            <slot></slot>
                        </view>
                    </view>
                </block>
            </block>
        </block>
    </block>
</block>