import Taro from "@tarojs/taro-h5";
import Nerv from "nervjs";
import { View } from "@tarojs/components";
import { classNames } from "../../lib/index";
export default class ClGrid extends Taro.Component {
  render() {
    const props = this.props;

    const colClassName = `col-${props.col}`;
    const squareClassName = props.mode === "square" ? "grid-square" : "";
    const gridComponent = <View className={classNames(`grid ${colClassName} ${squareClassName}`, props.className)} style={Object.assign({}, props.style)}>
      {this.props.children}
    </View>;
    return gridComponent;
  }

}
ClGrid.options = {
  addGlobalClass: true
};
ClGrid.defaultProps = {
  col: 3,
  mode: "normal"
};