import Nerv from "nervjs";
import { Text } from "@tarojs/components";
import Taro from "@tarojs/taro-h5";
import { classNames, isNumber } from "../../lib/index";
import { pxMap } from "../../lib/model";
export default class ClIcon extends Taro.Component {
  render() {
    const props = this.props;

    const iconName = props.iconName || "";
    let iconNameClass = `cuIcon-${props.iconName}`;
    if (props.other) {
      iconNameClass = iconName;
    }
    const sizeClassName = props.size || "normal";
    const bgColorClassName = props.color ? `text-${props.color}` : "";
    return <Text className={classNames(`${bgColorClassName} ${iconNameClass}`, props.className)} style={Object.assign({
      fontSize: `${isNumber(sizeClassName) ? Taro.pxTransform(sizeClassName) : Taro.pxTransform(pxMap[sizeClassName])}`
    }, props.style)} />;
  }

}
ClIcon.options = {
  addGlobalClass: true
};